# 0702-basebackend-salesmartly-compatible-api

Purpose: add a Salesmartly-compatible `/api/v2` customer-service external API while preserving the existing HMAC customer-service API, and support admin-facing current-origin URLs for integration documentation.
