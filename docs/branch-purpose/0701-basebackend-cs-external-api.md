# 0701-basebackend-cs-external-api

Purpose: add shared customer-service external API support and mobile QR landing reservations.

Key scope:
- External anonymous customer-service sessions with HMAC-signed API access and polling replies.
- Customer-service QR landing page metadata and reservation tokens that bind the page to the same assigned support account.
- Public reservation endpoint and admin credential management endpoints for host projects.
