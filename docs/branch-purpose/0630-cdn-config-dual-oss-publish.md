# 0630-cdn-config-dual-oss-publish

This branch preserves existing node OSS endpoint and credential fields when admin update requests send blank values, so UI forms can leave the global OSS configuration empty without clearing stored CDN publishing settings.
